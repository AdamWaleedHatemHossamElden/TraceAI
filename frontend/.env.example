# Development: Vite proxy forwards /api/* to http://localhost:5000/api/*
# Keep this value as /api when running locally with the Vite dev server.
# Override with the full URL (e.g. https://api.traceai.com/api) for production.
VITE_API_BASE_URL=/api
